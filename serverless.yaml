provider:
  name: aws
  runtime: python3.9
  region: us-east-1
functions:
  theLambda:
    name: lambda_function
    handler: lambda.handler
    description: lambda-function
    timeout: 5
    memorySize: 128
    
